# Codex repository bootstrap

Before working in this repository, read `docs/agent-workflow.md`, locate the
relevant note under `docs/work/`, and read `CLAUDE.md` for Laravel Boost and
Tablet-specific guidance.

Reconcile the Work note with `git status` and relevant diffs before acting.
Preserve unrelated worktree changes. Implementation authorization does not
authorize commit or push; terminal delivery requires separate explicit Human
authorization.
